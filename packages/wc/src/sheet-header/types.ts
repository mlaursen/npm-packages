export interface SheetHeaderProperties {
  /**
   * @defaultValue `"Back"`
   */
  backLabel?: string;
  backButton?: boolean;

  /**
   * @defaultValue `"Close"`
   */
  closeLabel?: string;
  closeButton?: boolean;
}
