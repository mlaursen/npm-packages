---
layout: docs

eleventyNavigation:
  key: Card
  parent: Home

page:
  title: Card

components:
  - name: mwc-clickable-card
    title: Clickable
  - name: mwc-clickable-card
    title: Disabled
    disabled: true
  - name: mwc-link-card
    title: Link
  - name: mwc-card
    title: Non-interactive
---

<mwc-box stacked align="stretch" padding="none">
  {% for variant in card.variants %}
    <mwc-typography variant="headline" text-transform="capitalize" margin="top">
      <h3>{{ variant }}</h3>
    </mwc-typography>
    <mwc-box grid align="stretch" style="--mwc-box-item-min-size: 16rem">
      {% for component in components %}
        <{{ component.name }} variant="{{ variant }}" {{ "disabled" if component.disabled }}>
        <mwc-box stacked align="start">
          <mwc-typography variant="display" size="medium" margin="none">
            <h3>{{ component.title }}</h3>
          </mwc-typography>
          <mwc-typography variant="title" margin="none">
            <h4>Subhead</h4>
          </mwc-typography>
          <mwc-typography margin="none">
            <p>Explain more about the topic shown in the medium display and subhead through supporting text here.</p>
          </mwc-typography>
        </mwc-box>
        </{{ component.name }}>
      {% endfor %}
    </mwc-box>
  {% endfor %}
</mwc-box>
<mwc-divider>
</mwc-divider>
<mwc-typography variant="display">
  <h2>Drag and Drop Example</h2>
</mwc-typography>
<mwc-box id="dnd-area">
  {% for i in [1, 2, 3, 4] %}
    <mwc-clickable-card draggable="true">
      <mwc-box>
        <mwc-typography variant="title">
          <h4>Drag Me {{ i }}</h4>
        </mwc-typography>
      </mwc-box>
    </mwc-clickable-card>
  {% endfor %}
</mwc-box>
{% js "defer" %}
{% include "scripts/wc/card.js" %}
{% endjs %}
